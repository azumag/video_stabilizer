{
  "name": "twitch-video-stabilizer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phase 0 Chrome extension for Twitch video stabilization feasibility testing",
  "license": "GPL-2.0-only",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm run validate && npm test && npm run build"
  }
}
